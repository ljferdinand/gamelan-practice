[package]
name = "gamelan-practice"
version = "0.1.0"
description = "Self-calibrating gamelan practice tool"
edition = "2021"
rust-version = "1.77"

[build-dependencies]
tauri-build = { version = "2" }

[dependencies]
tauri = { version = "2" }

[profile.release]
opt-level = "s"
lto = true
strip = true
